Функция ВернутьТест(Наименование) Экспорт

	Нс = Справочники.ксТесты.НайтиПоНаименованию(Наименование);
	Если не ЗначениеЗаполнено(нс) Тогда
		СО = Справочники.ксТесты.СоздатьЭлемент();
		СО.Наименование = Наименование;
		СО.Записать();
		нс = СО.Ссылка;
	КонецЕсли;
	
	возврат Нс;	
	
КонецФункции
